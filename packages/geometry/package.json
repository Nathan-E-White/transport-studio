{
  "name": "@transport/geometry",
  "type": "module",
  "private": true,

  "dependencies": {
    "@transport/domain": "workspace:*",
    "@transport/shared": "workspace:*"
  },

  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  }
}
